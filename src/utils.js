export const randomNumBetween = (min, max) => Math.floor(Math.random() * (max - min + 1)) + min;

export const colors = {
  new: "lightblue",
  playing: "deepskyblue",
  won: "lightgreen",
  lost: "lightcoral"
};
